# Supabase 프로젝트 설정 > API 에서 복사해 .env.local 로 채운다.
# 실제 값은 절대 커밋하지 않는다.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# 선택. OG 태그의 절대 URL 기준값.
# 비워두면 Vercel 시스템 변수로 자동 판정하고, 로컬은 http://localhost:3000 이 된다.
# 도메인을 사거나 로컬을 터널로 열어 카카오 미리보기를 확인할 때만 채운다.
SITE_URL=
